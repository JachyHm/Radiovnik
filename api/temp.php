<?php
require_once "../simp_db.php";
require_once "utils.php";

/*$sql = $mysqli->prepare('UPDATE `transactions_channels` SET `gid` = ? WHERE `sr70` = ? AND `type` = ? AND `channel` = ?');
$result = $mysqli->query('SELECT * FROM `channels`');
if (!empty($result)) {
    while ($row = $result->fetch_assoc()) {
        $sql->bind_param("iiis", $row["gid"], $row["sr70"], $row["type"], $row["channel"]);
        $sql->execute();
    }
}*/
?>